import { createTheme } from "@mui/material";

const customeTheme = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#009FFD"
        },
        secondary: {
            main: "#B0E0E6"
        }
    }
})

export default customeTheme;